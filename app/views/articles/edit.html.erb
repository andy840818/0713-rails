<h1> 更新文章</h1>

<%= form_with(model: @article, data:{ trubo: false } ) do |f| %>

  
  <%= f.label :title, '標題' %>
  <%= f.text_field :title %>
 
 <div>
  <%= f.label :content, '內文'%>
  <%= f.text_area :content %>
 </div>
 <div>
  <%= f.submit %>
 </div>
<% end %>

